# Настройка DeepSeek

## О DeepSeek

DeepSeek - это китайская LLM модель, известная хорошим качеством и скоростью. Доступна через OpenGate и Ollama.

## 🚀 Вариант 1: DeepSeek через OpenGate (рекомендуется)

### Преимущества:
- ✅ Работает без VPN
- ✅ Не требует установки
- ✅ Быстрый доступ

### Настройка:

1. **Получите API ключ OpenGate** (см. `docs/opengate-api-key-quick.md`)

2. **Обновите файл `.env`:**

```env
# OpenGate настройки для DeepSeek
LLM_PROVIDER=openai
OPENAI_BASE_URL=https://api.opengatellm.ru/v1
LLM_MODEL=deepseek-chat
OPENAI_API_KEY=your_opengate_api_key_here

# RAGAS LLM (можно использовать ту же модель или другую)
RAGAS_LLM_MODEL=deepseek-chat
```

**Доступные модели DeepSeek в OpenGate:**
- `deepseek-chat` ⭐ (рекомендуется для чата)
- `deepseek-coder` (специализирована для кода)

3. **Проверьте работу:**

```bash
make run
```

---

## 💻 Вариант 2: DeepSeek через официальный API (работает в России)

### Преимущества:
- ✅ Работает без VPN
- ✅ Официальный API от DeepSeek
- ✅ Хорошее качество

### Настройка:

1. **Получите API ключ:**
   - Зарегистрируйтесь на https://www.deepseek.ru/
   - Получите API ключ в личном кабинете

2. **Обновите файл `.env`:**

```env
# DeepSeek официальный API
LLM_PROVIDER=openai
OPENAI_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
OPENAI_API_KEY=your_deepseek_api_key_here

# RAGAS LLM
RAGAS_LLM_MODEL=deepseek-chat
```

3. **Запустите бота:**

```bash
make run
```

**Примечание:** Проверьте актуальный endpoint и название модели в документации DeepSeek.

---

## 📊 Сравнение вариантов

| Параметр | OpenGate | Официальный API |
|----------|----------|-----------------|
| **Стоимость** | Бесплатно (есть лимиты) | Бесплатно (есть лимиты) |
| **VPN** | Не нужен | Не нужен |
| **Интернет** | Нужен | Нужен |
| **Приватность** | Данные уходят в API | Данные уходят в API |
| **Скорость** | Быстро | Быстро |
| **Установка** | Не требуется | Не требуется |
| **Регистрация** | Нужен OpenGate ключ | Нужен DeepSeek ключ |

---

## 🎯 Рекомендации

### Для быстрого тестирования:
Используйте **OpenGate** - не нужно ничего скачивать, просто укажите модель в `.env`

### Для официального API:
Используйте **официальный DeepSeek API** - прямой доступ к модели, может быть быстрее

---

## ⚠️ Важно

1. **DeepSeek через OpenGate:**
   - Убедитесь, что модель `deepseek-chat` доступна в OpenGate (проверьте https://opengatellm.ru/catalog.html)
   - Модель может называться по-другому, например `deepseek/deepseek-chat`

2. **DeepSeek через официальный API:**
   - Требуется регистрация на deepseek.ru
   - Проверьте актуальный endpoint в документации
   - Может быть бесплатный tier с лимитами

---

## 🔍 Проверка доступности

### OpenGate:
1. Откройте: https://opengatellm.ru/catalog.html
2. Найдите "DeepSeek" в списке моделей
3. Проверьте точное название модели

### Официальный API:
1. Проверьте документацию: https://www.deepseek.ru/
2. Убедитесь, что API доступен в России
3. Проверьте актуальный endpoint и название модели

---

## 📝 Пример полной настройки для DeepSeek

### Через OpenGate:

```env
# Обязательные
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_opengate_api_key_here

# LLM (DeepSeek через OpenGate)
LLM_PROVIDER=openai
OPENAI_BASE_URL=https://api.opengatellm.ru/v1
LLM_MODEL=deepseek-chat
RAGAS_LLM_MODEL=deepseek-chat

# Embeddings (можно оставить Ollama или HuggingFace)
EMBEDDINGS_PROVIDER=ollama
EMBEDDINGS_MODEL=nomic-embed-text

# Остальные настройки
SYSTEM_ROLE=банковский ассистент
CONTEXT_TURNS=8
RETRIEVER_K=4
DATA_PATH=@data
LOG_LEVEL=INFO
```

### Через официальный API:

```env
# Обязательные
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_deepseek_api_key_here

# LLM (DeepSeek официальный API)
LLM_PROVIDER=openai
OPENAI_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
RAGAS_LLM_MODEL=deepseek-chat

# Embeddings
EMBEDDINGS_PROVIDER=ollama
EMBEDDINGS_MODEL=nomic-embed-text

# Остальные настройки
SYSTEM_ROLE=банковский ассистент
CONTEXT_TURNS=8
RETRIEVER_K=4
DATA_PATH=@data
LOG_LEVEL=INFO
```

---

## 🚀 Быстрый старт

1. Выберите вариант (OpenGate или официальный API)
2. Получите API ключ (OpenGate или DeepSeek)
3. Обновите `.env` файл
4. Запустите бота: `make run`
5. Протестируйте в Telegram

---

## ❓ Проблемы?

Если модель не работает:

1. **OpenGate:**
   - Проверьте, что модель доступна в каталоге OpenGate
   - Убедитесь, что API ключ правильный
   - Проверьте точное название модели

2. **Официальный API:**
   - Убедитесь, что API ключ правильный
   - Проверьте актуальный endpoint в документации
   - Убедитесь, что API доступен в России

