# Настройка Groq

Groq предоставляет OpenAI-совместимый API, поэтому достаточно указать URL, модель и API ключ.

## 1. Получите Groq API ключ

1. Перейдите на <https://console.groq.com/>
2. Создайте аккаунт или войдите
3. Откройте раздел **API Keys**
4. Нажмите **Create Key** и скопируйте значение (ключ показывается только один раз!)

## 2. Обновите `.env`

```env
# Groq
LLM_PROVIDER=openai
OPENAI_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL=llama-3.1-8b-instant
OPENAI_API_KEY=your_groq_api_key_here

# RAGAS (можно использовать ту же модель)
RAGAS_LLM_MODEL=llama-3.1-8b-instant
```

> 💡 Модель можно заменить на любую доступную в Groq, например `llama-3.1-70b-versatile` или `mixtral-8x7b-32768`.

## 3. Проверьте подключение

```powershell
# Windows PowerShell
$env:GROQ_API_KEY = "your_groq_api_key_here"
uv run python scripts/test_groq.py
```

Ожидаемый вывод:

```
=== Проверка доступа к Groq ===
Base URL: https://api.groq.com/openai/v1
Model: llama-3.1-8b-instant
...
✅ Успешно! Ответ Groq: 'Работает'
```

## 4. Запустите бота

```bash
make run
```

Бот начнёт использовать Groq вместо Fireworks.

## 5. Подсказки

- Если видите `403 Access denied`, попробуйте запустить скрипт с сервера вне РФ или через Render.
- Если модель долго отвечает, уменьшите `LLM_MODEL` до `llama-3.1-8b-instant`.
- Для деплоя на Render можно использовать те же переменные окружения в панели Render.

---

См. также:
- `scripts/test_groq.py` — быстрый тест доступа
- `docs/deployment-render.md` — деплой на Render с Groq


