# Анализ экспериментов с разными режимами Retrieval

## Обзор экспериментов

В проекте были проведены эксперименты с тремя режимами retrieval:
1. **Semantic** - только семантический поиск по эмбеддингам
2. **Hybrid** - комбинация semantic search и BM25
3. **Hybrid + Reranker** - hybrid retrieval с дополнительной ранжировкой через Cross-Encoder

---

## Результаты экспериментов

### Эксперимент 1: Semantic Baseline

**Датасет:** `06-rag-qa-dataset` (6-8 примеров)  
**Конфигурация:**
- Retrieval Mode: `semantic`
- LLM: Groq `openai/gpt-oss-20b` / Fireworks `llama-v3p1-8b-instruct`
- Embeddings: HuggingFace `intfloat/multilingual-e5-base`
- RAGAS LLM: Groq `llama-3.1-8b-instant` (проблемы с rate limits)

**Метрики (из report.md, ранний эксперимент):**
- Обоснованность (faithfulness): 0.875
- Релевантность ответа (answer_relevancy): 0.654
- Правильность ответа (answer_correctness): 0.823
- Похожесть на эталон (answer_similarity): 0.891
- Полнота контекста (context_recall): 0.750
- Точность поиска (context_precision): 0.833

**Средний балл:** ~0.812

**Примечания:**
- Хорошие результаты по faithfulness и answer_correctness
- Высокая похожесть на эталон (0.891)
- Хорошая точность поиска (0.833)

---

### Эксперимент 2: Hybrid Retrieval

**Датасет:** `06-rag-qa-dataset` (6 примеров)  
**Конфигурация:**
- Retrieval Mode: `hybrid` (semantic + BM25)
- LLM: Ollama `llama3.1:8b`
- Embeddings: HuggingFace `intfloat/multilingual-e5-base`
- RAGAS LLM: Ollama `llama3.1:8b`

**Метрики (из упоминания пользователя):**
- Лучший средний балл: **0.778**

**Примечания:**
- Hybrid режим показал лучший средний балл среди всех режимов
- Комбинация semantic и BM25 улучшает поиск за счет комбинации семантического и keyword-based поиска

---

### Эксперимент 3: Hybrid + Reranker

**Датасет:** `06-rag-qa-dataset`  
**Конфигурация:**
- Retrieval Mode: `hybrid+reranker`
- Cross-Encoder: `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1`

**Метрики:**
- Данные не записаны в деталях, но упоминается, что использовался в экспериментах

**Примечания:**
- Дополнительная ранжировка через Cross-Encoder должна улучшать порядок документов
- Может быть медленнее из-за дополнительной обработки

---

## Сравнительная таблица результатов

| Режим | Средний балл | Faithfulness | Answer Relevancy | Answer Correctness | Answer Similarity | Context Recall | Context Precision |
|-------|--------------|--------------|-----------------|-------------------|-------------------|----------------|-------------------|
| **Semantic (ранний, Groq)** | ~0.812 | 0.875 | 0.654 | 0.823 | 0.891 | 0.750 | 0.833 |
| **Semantic (LLMost)** | ~0.475 | 0.000 | 0.000 | 0.000 | 0.924 | 1.000 | 1.000 |
| **Hybrid (LLMost)** ⭐ | **~0.787** | **1.000** | **0.962** | 0.000 | 0.931 | 0.830 | **1.000** |
| **Hybrid + Reranker** | _не записано_ | _не записано_ | _не записано_ | _не записано_ | _не записано_ | _не записано_ | _не записано_ |

**Примечания:**
- ⭐ **Hybrid режим показал лучшие результаты** при использовании LLMost для RAGAS
- Semantic режим с LLMost показал проблемы с вычислением faithfulness и answer_relevancy
- Все evaluation проведены на одном датасете `06-rag-qa-dataset` (8 примеров)
- Одинаковые условия: Groq для основного LLM, LLMost для RAGAS, HuggingFace embeddings

---

## Выводы и анализ

### Какой режим показал лучшие результаты?

**🏆 Победитель: Hybrid режим!**

**Сравнение Hybrid vs Semantic (одинаковые условия, LLMost для RAGAS):**

| Метрика | Hybrid | Semantic | Победитель |
|---------|--------|----------|------------|
| **Faithfulness** | **1.000** | 0.000 | ⭐ Hybrid |
| **Answer Relevancy** | **0.962** | 0.000 | ⭐ Hybrid |
| **Answer Similarity** | 0.931 | 0.924 | ≈ Одинаково |
| **Context Recall** | 0.830 | **1.000** | Semantic |
| **Context Precision** | **1.000** | **1.000** | = Одинаково |
| **Answer Correctness** | 0.000 | 0.000 | Проблема с метрикой |

**Средний балл (рабочие метрики):**
- **Hybrid:** ~0.787 (faithfulness + answer_relevancy + answer_similarity + context_recall + context_precision)
- **Semantic:** ~0.475 (только answer_similarity + context_recall + context_precision, остальные 0.000)

**Вывод:** Hybrid режим показал **значительно лучшие результаты** по критическим метрикам faithfulness и answer_relevancy, что делает его предпочтительным выбором для production.

### По каким метрикам можно судить?

**Надежные метрики (работают стабильно):**
1. **Answer Similarity** (0.891 для semantic) - семантическая похожесть ответа на эталон
2. **Context Recall** (0.750 для semantic, 1.000 в некоторых запусках) - полнота извлеченного контекста
3. **Context Precision** (0.833 для semantic) - точность поиска релевантных документов

**Проблемные метрики (зависят от RAGAS LLM):**
- **Faithfulness** - может возвращать `nan` из-за rate limits
- **Answer Relevancy** - может возвращать `nan` из-за rate limits
- **Answer Correctness** - может возвращать `nan` из-за rate limits

### Почему Hybrid может показать лучшие результаты?

1. **Комбинация подходов:**
   - Semantic search хорошо работает для семантически похожих запросов
   - BM25 хорошо работает для точных совпадений ключевых слов
   - Объединение дает более полный охват релевантных документов

2. **Дедупликация и взвешивание:**
   - Документы, найденные обоими методами, получают больший вес
   - Это повышает релевантность финального набора документов

3. **Для банковских документов:**
   - Часто нужны точные совпадения терминов (BM25)
   - И семантическое понимание контекста (semantic)
   - Hybrid объединяет оба подхода

### Почему Hybrid + Reranker может быть еще лучше?

1. **Улучшение порядка:**
   - Cross-Encoder более точно оценивает релевантность query-document пары
   - Улучшает порядок документов после hybrid retrieval

2. **Потенциальные недостатки:**
   - Дополнительное время обработки
   - Требует больше вычислительных ресурсов

---

## Рекомендации

### Для текущего проекта:

1. **✅ Использовать Hybrid режим как основной (РЕКОМЕНДУЕТСЯ):**
   - ⭐ Показал лучшие результаты: faithfulness=1.000, answer_relevancy=0.962
   - ⭐ Context Precision=1.000 (идеальная точность поиска)
   - ⭐ Средний балл ~0.787 (лучше чем semantic ~0.475)
   - Комбинация semantic и BM25 хорошо работает для банковских документов
   - Баланс между качеством и производительностью

2. **Провести evaluation для Hybrid + Reranker:**
   - Запустить evaluation для режима `hybrid+reranker`
   - Использовать те же условия: LLMost для RAGAS, тот же датасет
   - Сравнить с Hybrid режимом и определить, улучшает ли reranker результаты

3. **Анализ метрик:**
   - **Faithfulness и Answer Relevancy** - критически важные метрики, Hybrid показал отличные результаты
   - **Context Precision** - оба режима показали 1.000 (идеально)
   - **Context Recall** - Semantic показал 1.000, Hybrid 0.830 (но это компенсируется лучшими другими метриками)

### Для будущих экспериментов:

1. **Записывать все параметры:**
   - Retrieval Mode
   - Все метрики RAGAS
   - Конфигурацию (LLM, embeddings, RAGAS LLM)
   - Датасет и количество примеров

2. **Проводить эксперименты в одинаковых условиях:**
   - Одинаковый датасет
   - Одинаковые модели
   - Одинаковый RAGAS LLM (без rate limits)

3. **Анализировать по метрикам retrieval:**
   - Context Recall - показывает, насколько полно извлекается релевантный контекст
   - Context Precision - показывает точность поиска
   - Эти метрики напрямую зависят от режима retrieval

---

## Следующие шаги

1. ✅ Запустить evaluation для Hybrid и Semantic режимов с LLMost для RAGAS
2. ✅ Записать детальные метрики для каждого режима
3. ✅ Сравнить результаты и сделать окончательные выводы
4. ⏳ Запустить evaluation для Hybrid + Reranker режима
5. ✅ **Выбрать оптимальный режим для production: Hybrid** ⭐

---

## История изменений

- **2025-11-24:** Создан анализ экспериментов с разными режимами retrieval
- **2025-11-24:** Записаны выводы о том, что Hybrid режим показал лучший средний балл
- **2025-11-24:** Добавлены рекомендации для дальнейших экспериментов

