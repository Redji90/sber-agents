# Следующие шаги

## ✅ Выполнено

Все итерации Спринта 3 (Мониторинг и оценка качества) завершены:
- ✅ Итерация 13: Рефакторинг RAG цепочки для возврата документов через RunnablePassthrough
- ✅ Итерация 14: Добавить опциональное отображение источников в ответе
- ✅ Итерация 15: Интегрировать LangSmith для автоматического трейсинга
- ✅ Итерация 16: Создать модуль синтеза датасетов (dataset_synthesizer.py)
- ✅ Итерация 17: Создать модуль evaluation (evaluation.py) с RAGAS метриками
- ✅ Итерация 18: Добавить команду /evaluate_dataset и интеграцию с LangSmith Feedback

## 📋 Рекомендуемые следующие действия

### 1. Обновление зависимостей
- ✅ Добавлены новые зависимости в `pyproject.toml`:
  - `ragas>=0.1.*` - для вычисления метрик
  - `datasets>=2.14.*` - для работы с датасетами
  - `langsmith>=0.1.*` - для работы с LangSmith API
  - `langchain-huggingface>=0.0.*` - опционально, для HuggingFace эмбеддингов

**Действие**: Выполнить `make install` для установки новых зависимостей

### 2. Настройка переменных окружения
- ✅ Создан `.env.example` с примерами всех переменных окружения
- ⚠️ Нужно создать `.env` файл с реальными значениями

**Действие**: 
1. Скопировать `.env.example` в `.env`
2. Заполнить обязательные переменные:
   - `TELEGRAM_BOT_TOKEN`
   - `OPENAI_API_KEY`
3. Опционально заполнить переменные для мониторинга:
   - `LANGSMITH_API_KEY` - для трейсинга и evaluation
   - `LANGSMITH_PROJECT` - название проекта в LangSmith
   - `SHOW_SOURCES` - показывать ли источники в ответе

### 3. Тестирование базовой функциональности
**Действия**:
1. Запустить бота: `make run`
2. Проверить команды:
   - `/start` - должно работать
   - `/help` - должно показывать все команды
   - `/index` - должно запускать индексацию
   - `/index_status` - должно показывать статус
3. Проверить RAG:
   - Отправить текстовое сообщение с вопросом
   - Проверить, что бот отвечает на основе документов
   - При `SHOW_SOURCES=true` проверить отображение источников

### 4. Тестирование мониторинга (требуется LangSmith API ключ)
**Действия**:
1. Установить `LANGSMITH_API_KEY` в `.env`
2. Запустить бота
3. Отправить несколько вопросов
4. Проверить в LangSmith UI, что запросы трейсятся

### 5. Тестирование синтеза датасетов
**Действия**:
1. Убедиться, что в `@data/` есть PDF файлы
2. Запустить синтез датасета (можно через отдельный скрипт или добавить команду бота)
3. Проверить создание файла `datasets/06-rag-qa-dataset.json`
4. Если установлен `LANGSMITH_API_KEY`, проверить загрузку датасета в LangSmith

### 6. Тестирование evaluation
**Действия**:
1. Убедиться, что есть датасет в LangSmith (можно синтезировать через итерацию 5)
2. Запустить команду `/evaluate_dataset` в боте
3. Проверить, что evaluation выполняется и возвращает метрики
4. Проверить загрузку результатов в LangSmith

### 7. Проверка кода
**Действия**:
1. Запустить линтер: `make lint`
2. Запустить форматтер: `make format`
3. Исправить найденные ошибки

### 8. Документация
- ✅ Обновлён `README.md` с описанием новых функций
- ✅ Обновлены `docs/idea.md` и `docs/vision.md`
- ✅ Обновлён `docs/tasklist.md`

## 🐛 Известные проблемы и улучшения

### Потенциальные проблемы
1. **Синхронный вызов в evaluation**: В `evaluation.py` используется `rag_chain.invoke()` (синхронный), хотя цепочка поддерживает async. Это должно работать через `asyncio.to_thread`, но можно улучшить для производительности.

2. **Загрузка feedback в LangSmith**: Реализация загрузки feedback в LangSmith упрощена (только логирование). Можно расширить для полноценной загрузки метрик для каждого примера.

3. **Обработка ошибок в evaluation**: При ошибке на одном примере evaluation продолжается с пустыми значениями. Можно улучшить обработку ошибок.

### Возможные улучшения
1. **Команда для синтеза датасетов**: Можно добавить команду `/synthesize_dataset` в бота для удобства.

2. **Параметры evaluation**: Можно добавить возможность указать датасет через параметры команды `/evaluate_dataset <dataset_name>`.

3. **Кэширование результатов**: Можно кэшировать результаты evaluation для повторного использования.

4. **Визуализация метрик**: Можно добавить визуализацию метрик (графики, диаграммы) в ответ бота.

## 📝 Коммит изменений

После проверки всех шагов рекомендуется сделать коммит:

```bash
git add .
git commit -m "feat(sprint-3): добавлена система мониторинга и оценки качества

- Рефакторинг RAG цепочки для возврата документов через RunnablePassthrough
- Добавлено опциональное отображение источников в ответе (SHOW_SOURCES)
- Интеграция LangSmith для автоматического трейсинга
- Модуль синтеза датасетов (dataset_synthesizer.py)
- Модуль evaluation с RAGAS метриками (evaluation.py)
- Команда /evaluate_dataset для запуска evaluation
- Обновлены зависимости: ragas, datasets, langsmith, langchain-huggingface
- Обновлена документация"
```

## 🎯 Приоритеты

1. **Высокий приоритет**:
   - Установка зависимостей (`make install`)
   - Настройка `.env` файла
   - Тестирование базовой функциональности

2. **Средний приоритет**:
   - Тестирование мониторинга (требуется LangSmith API ключ)
   - Тестирование синтеза датасетов
   - Тестирование evaluation

3. **Низкий приоритет**:
   - Улучшения обработки ошибок
   - Дополнительные команды бота
   - Визуализация метрик


