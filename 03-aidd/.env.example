# .env.example
# Примеры переменных окружения для запуска LLM-ассистента.
# Скопируйте этот файл в .env и заполните своими значениями.

# --- Обязательные переменные ---

# Токен вашего Telegram-бота, полученный от BotFather.
TELEGRAM_BOT_TOKEN=ВАШ_ТЕЛЕГРАМ_БОТ_ТОКЕН

# Ключ API для доступа к OpenRouter.
OPENROUTER_API_KEY=ВАШ_OPENROUTER_API_KEY

# --- Необязательные переменные (значения по умолчанию) ---

# Базовый URL для API OpenRouter (по умолчанию: https://openrouter.ai/api/v1).
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Модель LLM для использования (по умолчанию: gemini-2.5-flash).
LLM_MODEL=gemini-2.5-flash

# Роль системного промпта для LLM (по умолчанию: банковский ассистент).
SYSTEM_ROLE="банковский ассистент"

# Количество последних реплик для хранения в контексте диалога (по умолчанию: 8).
CONTEXT_TURNS=8

# Уровень логирования (по умолчанию: INFO). Возможные значения: DEBUG, INFO, WARNING, ERROR, CRITICAL.
LOG_LEVEL=INFO